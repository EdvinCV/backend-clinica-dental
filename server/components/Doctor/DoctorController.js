// Modelos
const db = require('../../config/dbconnection');
const Doctor = db.doctores;

// Obtener doctor
exports.getDoctor = async (req, res) => {
    try {
        const {id} = req.body;

        const doctor = await 
    }
}

// Obtener doctores
// Crear doctor
// Editar doctor
// Eliminar doctor